@model List<WebBanHangOnline.Models.EF.Post>

@if (Model != null && Model.Any())
{
    foreach (var item in Model)
    {
        <div class="col-lg-4 blog_item_col">
            <div class="blog_item">
                <div class="blog_background" style="background-image:url(@item.Image)"></div>
                <div class="blog_content d-flex flex-column align-items-center justify-content-center text-center">
                    <h4 class="blog_title">@item.Title</h4>
                    <span class="blog_meta">@item.CreatedDate</span>
                    <a class="blog_more" href="/BLog/Delai_blog/@item.id">Read more</a>
                </div>
            </div>
        </div>
    }
}
